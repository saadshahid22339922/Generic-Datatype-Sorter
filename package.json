{
  "name": "sort",
  "version": "1.0.0",
  "description": "For Creation of This Project 1) Run \"tsc --init\" 2) Change setting in tsconfig.json file for src and build 3) To add watcher run with \"tsc -w\"",
  "main": "index.js",
  "dependencies": {
    "concurrently": "^6.2.0",
    "nodemon": "^2.0.8"
  },
  "devDependencies": {},
  "scripts": {
    "start:build": "tsc -w",
    "start:run": "nodemon build/index.js",
    "start": "concurrently npm:start:*"
  },
  "author": "",
  "license": "ISC"
}
